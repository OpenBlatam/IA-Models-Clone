# Caching System Dependencies
# ===========================

# Core caching dependencies
redis>=4.5.0                    # Redis client for Python
cachetools>=5.3.0              # In-memory caching with TTL, LRU, LFU
structlog>=23.1.0              # Structured logging

# FastAPI integration
fastapi>=0.104.0               # FastAPI framework
pydantic>=2.4.0                # Data validation and settings management

# Async support
asyncio-mqtt>=0.13.0           # Async MQTT support (optional)
aiofiles>=23.2.0               # Async file operations

# Serialization and compression
msgpack>=1.0.5                 # Fast binary serialization (optional)
orjson>=3.9.0                  # Fast JSON serialization (optional)

# Monitoring and metrics
prometheus-client>=0.19.0      # Prometheus metrics (optional)
psutil>=5.9.0                  # System and process utilities

# Testing dependencies
pytest>=7.4.0                  # Testing framework
pytest-asyncio>=0.21.0         # Async testing support
pytest-mock>=3.11.0            # Mocking support for tests
pytest-cov>=4.1.0              # Coverage reporting

# Development dependencies
black>=23.9.0                  # Code formatting
isort>=5.12.0                  # Import sorting
flake8>=6.1.0                  # Linting
mypy>=1.5.0                    # Type checking

# Optional: Performance monitoring
memory-profiler>=0.61.0        # Memory usage profiling
line-profiler>=4.1.0           # Line-by-line profiling

# Optional: Redis cluster support
redis-py-cluster>=2.1.3        # Redis cluster support

# Optional: Redis Sentinel support
redis-sentinel>=0.1.0          # Redis Sentinel support

# Optional: Cache backends
diskcache>=5.6.0               # Disk-based caching (optional)
memcached>=1.59                # Memcached support (optional)

# Optional: Compression
lz4>=4.3.0                     # LZ4 compression (optional)
zstandard>=0.21.0              # Zstandard compression (optional)

# Optional: Encryption
cryptography>=41.0.0           # Encryption for sensitive cached data

# Optional: Distributed locking
redlock-py>=1.0.0              # Distributed locking with Redis

# Optional: Cache warming and background tasks
celery>=5.3.0                  # Background task processing
flower>=2.0.0                  # Celery monitoring

# Optional: Health checks
healthcheck>=1.3.0             # Health check utilities

# Optional: Configuration management
python-decouple>=3.8           # Environment variable management
dynaconf>=3.2.0                # Dynamic configuration

# Optional: Logging and monitoring
loguru>=0.7.0                  # Advanced logging
sentry-sdk>=1.32.0             # Error tracking and monitoring

# Optional: Performance testing
locust>=2.15.0                 # Load testing
wrk>=0.1.0                     # HTTP benchmarking tool

# Optional: Cache analysis tools
cache-analyzer>=0.1.0          # Cache performance analysis
cache-visualizer>=0.1.0        # Cache usage visualization

# Production dependencies
gunicorn>=21.2.0               # WSGI HTTP Server
uvicorn[standard]>=0.23.0      # ASGI server 