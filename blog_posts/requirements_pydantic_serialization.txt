# Pydantic Serialization System Dependencies
# Core dependencies
pydantic>=2.0.0
structlog>=23.0.0

# Serialization formats
# JSON (built-in)
# Pickle (built-in)

# Optional fast serialization formats
orjson>=3.9.0  # Fast JSON serialization
msgpack>=1.0.0  # MessagePack serialization

# Compression
# gzip (built-in)

# Caching (if using external caching system)
redis>=4.5.0  # Redis client for external caching
aioredis>=2.0.0  # Async Redis client

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# Development
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0
flake8>=6.0.0

# Performance monitoring
psutil>=5.9.0  # System monitoring
memory-profiler>=0.60.0  # Memory profiling

# Optional: FastAPI integration
fastapi>=0.100.0
uvicorn>=0.22.0

# Optional: Database integration
sqlalchemy>=2.0.0
asyncpg>=0.28.0  # PostgreSQL async driver

# Optional: HTTP client for external API integration
httpx>=0.24.0

# Optional: Configuration management
python-dotenv>=1.0.0

# Optional: Metrics and monitoring
prometheus-client>=0.17.0 