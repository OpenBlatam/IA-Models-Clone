# ============================================================================
# Dermatology AI - Docker-Optimized Dependencies
# Optimized for containerized deployments
# Strips unnecessary packages, optimizes for multi-stage builds
# ============================================================================

# Core FastAPI Stack
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
starlette>=0.41.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.12

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=43.0.0
slowapi>=0.1.9
itsdangerous>=2.2.0

# Database (PostgreSQL - most common in containers)
sqlalchemy>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.10
sqlalchemy-utils>=0.41.2

# Caching
redis[hiredis]>=5.2.0
hiredis>=2.3.2
aiocache>=0.12.2

# HTTP Clients
httpx>=0.27.2
requests>=2.32.0

# Observability (Essential for containers)
prometheus-client>=0.21.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.47b0
opentelemetry-exporter-otlp>=1.27.0
structlog>=24.4.0
python-json-logger>=3.2.1

# Performance
uvloop>=0.20.0
orjson>=3.10.10
psutil>=6.1.0

# Image Processing
Pillow>=11.0.0
numpy>=2.1.0
opencv-python-headless>=4.10.0        # Headless version (no GUI deps)

# ML Inference (ONNX for containers)
onnx>=1.16.0
onnxruntime>=1.19.0

# Data Processing
pandas>=2.2.3
jinja2>=3.1.4

# Validation
email-validator>=2.2.0
python-dateutil>=2.9.0
pytz>=2024.2

# Utilities
tenacity>=9.0.0
python-slugify>=8.0.4
validators>=0.34.0

# Health Checks
healthcheck>=1.3.3

# Configuration
python-dotenv>=1.0.1
dynaconf>=3.3.0
pyyaml>=6.0.2

# ============================================================================
# Docker-Specific Optimizations:
# ============================================================================
# 1. Use opencv-python-headless (no X11/GUI dependencies)
# 2. Removed development tools
# 3. Removed testing frameworks
# 4. Removed visualization libraries (use external services)
# 5. Optimized for multi-stage builds
#
# Example Dockerfile:
#   FROM python:3.11-slim as builder
#   COPY requirements-docker.txt .
#   RUN pip install --user -r requirements-docker.txt
#
#   FROM python:3.11-slim
#   COPY --from=builder /root/.local /root/.local
#   ENV PATH=/root/.local/bin:$PATH
# ============================================================================



