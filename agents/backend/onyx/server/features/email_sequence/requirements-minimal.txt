# Core PyTorch and Deep Learning (Minimal)
torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0

# Data Processing (Minimal)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Web Interface (Minimal)
gradio>=3.35.0

# Configuration (Minimal)
python-dotenv>=1.0.0
pyyaml>=6.0

# Logging (Minimal)
loguru>=0.7.0

# File Handling (Minimal)
tqdm>=4.65.0
pathlib2>=2.3.7

# Validation (Minimal)
pydantic>=2.0.0

# Testing (Minimal)
pytest>=7.4.0

# Error Handling (Minimal)
better-exceptions>=0.3.0 