# ============================================================================
# Dermatology AI - Optimized Dependencies (Minimal Production)
# Stripped down version for production deployments
# Removes dev tools, optional features, and heavy ML dependencies
# ============================================================================

# Core FastAPI Stack
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
starlette>=0.41.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.12

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=43.0.0
slowapi>=0.1.9
itsdangerous>=2.2.0

# Database (PostgreSQL only - most common)
sqlalchemy>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.10
sqlalchemy-utils>=0.41.2

# Caching
redis[hiredis]>=5.2.0
hiredis>=2.3.2
aiocache>=0.12.2

# HTTP Clients
httpx>=0.27.2
requests>=2.32.0

# Observability (Essential only)
prometheus-client>=0.21.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-instrumentation-fastapi>=0.47b0
opentelemetry-exporter-otlp>=1.27.0
structlog>=24.4.0

# Performance
uvloop>=0.20.0
orjson>=3.10.10
psutil>=6.1.0

# Image Processing (Essential)
Pillow>=11.0.0
numpy>=2.1.0
opencv-python>=4.10.0

# ML Inference Only (No Training)
onnx>=1.16.0
onnxruntime>=1.19.0
# onnxruntime-gpu>=1.19.0  # Uncomment if GPU available

# Data Processing
pandas>=2.2.3
jinja2>=3.1.4

# Validation
email-validator>=2.2.0
python-dateutil>=2.9.0
pytz>=2024.2

# Utilities
tenacity>=9.0.0
python-slugify>=8.0.4
validators>=0.34.0

# Health Checks
healthcheck>=1.3.3

# Serverless
mangum>=0.18.1
aws-lambda-powertools>=2.40.0

# Configuration
python-dotenv>=1.0.1
dynaconf>=3.3.0
pyyaml>=6.0.2

# ============================================================================
# Removed for optimization:
# - Development tools (black, ruff, mypy, ipython)
# - Testing tools (pytest, etc.) - use requirements-dev.txt
# - Heavy ML training libraries (torch, transformers, etc.)
# - Optional databases (MongoDB, Cosmos DB, SQLite)
# - Message brokers (RabbitMQ, Kafka, Celery)
# - Visualization libraries
# - Experiment tracking (wandb, mlflow, tensorboard)
# - Multiple JSON libraries (keeping only orjson)
# ============================================================================



