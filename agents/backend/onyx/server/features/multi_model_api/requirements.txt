# ============================================================================
# Core FastAPI Framework
# ============================================================================
fastapi>=0.115.0,<0.117.0  # Modern async web framework
uvicorn[standard]>=0.32.0,<0.34.0  # ASGI server with HTTP/2 support
pydantic[email]>=2.9.0,<3.0.0  # Data validation with email support
pydantic-settings>=2.6.0,<3.0.0  # Settings management
pydantic-extra-types>=2.7.0  # Additional Pydantic types (IP, URL, etc)
python-multipart>=0.0.12  # Form data parsing

# ============================================================================
# Async HTTP Clients
# ============================================================================
httpx>=0.27.0,<0.29.0  # Modern async HTTP client (preferred over aiohttp)
# aiohttp>=3.11.0  # Alternative async HTTP client (optional, use httpx instead)

# ============================================================================
# WebSocket Support
# ============================================================================
websockets>=13.1,<14.0  # WebSocket implementation (updated version)

# ============================================================================
# Caching - Multi-tier with optimized libraries
# ============================================================================
cachetools>=5.4.0  # Fast in-memory caching with TTL support
diskcache>=5.6.3  # Fast disk-based cache (L3 tier)
redis[hiredis]>=5.2.0,<6.0.0  # Redis with hiredis parser for performance
aiocache>=0.12.2  # Async cache abstraction layer

# ============================================================================
# Serialization - Ultra-fast options
# ============================================================================
orjson>=3.10.0,<4.0.0  # Fastest JSON serialization (Rust-based, ~3-5x faster)
msgpack>=1.1.0,<2.0.0  # Binary serialization, faster than pickle
# rapidjson>=1.10  # Alternative (removed - orjson is better and more maintained)

# ============================================================================
# Performance Optimizations
# ============================================================================
uvloop>=0.20.0  # Ultra-fast asyncio event loop (Linux/macOS only)
aiofiles>=24.1.0  # Async file I/O operations

# ============================================================================
# Retry and Resilience Patterns
# ============================================================================
tenacity>=9.0.0,<10.0.0  # Retry logic with exponential backoff (preferred)
# backoff>=2.2.1  # Alternative (removed - tenacity is more feature-rich)
pybreaker>=1.0.1  # Circuit breaker pattern implementation

# ============================================================================
# Monitoring and Observability
# ============================================================================
prometheus-client>=0.21.0  # Metrics export for Prometheus
prometheus-fastapi-instrumentator>=7.0.0  # Automatic FastAPI instrumentation
psutil>=6.1.0  # System and process utilities
structlog>=24.4.0  # Structured logging (preferred over loguru for production)
structlog-sentry>=1.0.0  # Enhanced Sentry integration for structlog
# loguru>=0.7.2  # Alternative logging (removed - use structlog for consistency)

# OpenTelemetry for distributed tracing
opentelemetry-api>=1.28.0
opentelemetry-sdk>=1.28.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-httpx>=0.45b0  # HTTP client instrumentation

# Error tracking
sentry-sdk[fastapi]>=2.19.0  # Error tracking and monitoring

# ============================================================================
# Utilities
# ============================================================================
python-dotenv>=1.0.1  # Environment variable management
typing-extensions>=4.12.0  # Extended type hints support
click>=8.1.7  # CLI utilities
rich>=13.9.0  # Rich text and beautiful terminal formatting
python-dateutil>=2.9.0  # Date and time utilities
# pytz>=2023.3  # Timezone support (removed - use zoneinfo from stdlib in Python 3.9+)
email-validator>=2.2.0  # Email validation

# ============================================================================
# Database Support (Async) - Optional, include only if needed
# ============================================================================
# asyncpg>=0.29.0  # High-performance async PostgreSQL driver
# aiomysql>=0.2.0  # Async MySQL driver
# aiosqlite>=0.20.0  # Async SQLite driver
# motor>=3.6.0  # Async MongoDB driver

# ============================================================================
# Message Queues and Event Streaming - Optional, include only if needed
# ============================================================================
# aio-pika>=9.3.0  # Async RabbitMQ client
# kafka-python>=2.0.2  # Kafka client (Python)
# confluent-kafka>=2.5.0  # High-performance Kafka client (C-based)

# ============================================================================
# Security
# ============================================================================
cryptography>=43.0.0  # Cryptographic operations (updated for security)
passlib[bcrypt]>=1.7.4  # Password hashing
bcrypt>=4.2.0  # Secure password hashing
slowapi>=0.1.9  # Rate limiting for FastAPI
python-jose[cryptography]>=3.3.0  # JWT token handling (if needed for auth)

# ============================================================================
# Health Checks and Validation
# ============================================================================
# healthcheck>=1.3.3  # Health check utilities (removed - FastAPI has built-in health checks)

# ============================================================================
# Data Processing - Optional, include only if needed
# ============================================================================
# pandas>=2.2.0  # Data analysis and manipulation
# numpy>=1.26.0  # Numerical computing

# ============================================================================
# Development and Testing
# ============================================================================
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0  # Code coverage
pytest-benchmark>=4.0.0  # Performance benchmarking
pytest-mock>=3.14.0  # Mocking utilities
pytest-timeout>=2.3.0  # Timeout support for tests
pytest-xdist>=3.6.0  # Parallel test execution
pytest-httpx>=0.30.0  # Mocking httpx for tests
pytest-env>=1.1.0  # Environment variable management in tests
hypothesis>=6.120.0  # Property-based testing
httpx>=0.27.0  # Also needed for testing async HTTP clients

# Code quality and formatting
black>=24.8.0  # Code formatter
ruff>=0.6.0  # Fast Python linter (Rust-based, replaces flake8 + isort)
# isort>=5.13.0  # Import sorting (removed - ruff handles this)
mypy>=1.11.0  # Static type checking
pre-commit>=4.0.0  # Git hooks for code quality
bandit>=1.7.8  # Security linter
safety>=3.2.0  # Dependency vulnerability scanner
dependabot-alerts>=0.1.0  # Optional: GitHub Dependabot integration

# ============================================================================
# Documentation
# ============================================================================
mkdocs>=1.6.0  # Documentation generator
mkdocs-material>=10.0.0  # Material theme for MkDocs
mkdocs-swagger-ui-tag>=0.6.0  # Swagger UI for API docs

# ============================================================================
# Additional Useful Libraries
# ============================================================================
# Task queues (if needed for background jobs)
# celery>=5.4.0  # Distributed task queue
# redis>=5.2.0  # Already included above for caching

# Configuration management
pydantic-settings>=2.6.0  # Already included above

# API documentation
# fastapi-users>=12.0.0  # User management (if needed)
# fastapi-limiter>=0.1.6  # Alternative rate limiting

# Compression
# brotli>=1.1.0  # Brotli compression (faster than gzip)
# zstandard>=0.23.0  # Zstandard compression

# Metrics and profiling
# py-spy>=0.3.14  # Sampling profiler for Python
# memory-profiler>=0.61.0  # Memory usage profiler
